import { ParsedBlock } from '@src/components/blocks';
import React from 'react';

type Props = {
  block: ParsedBlock;
};

export const ProductQuery = ({ block }: Props) => {
  return <div>ProductQuery</div>;
};

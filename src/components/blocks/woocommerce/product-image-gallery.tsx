import { ProductGallery } from '@src/features/product/product-gallery';
import { BlockComponentProps } from '@src/components/blocks';

export const Gallery = ({ block }: BlockComponentProps) => {
  return <ProductGallery />;
};
